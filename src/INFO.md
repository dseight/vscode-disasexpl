This will probably help with loading of assembly
FileSystemProvider.readFile(uri: Uri): Uint8Array | Thenable<Uint8Array>;

Uint8Array has a method toString() that retuturns a string


workspace.openTextDocument()