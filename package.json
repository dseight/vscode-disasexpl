{
    "name": "disasexpl",
    "displayName": "disasexpl",
    "description": "",
    "version": "0.0.1",
    "publisher": "dseight",
    "engines": {
        "vscode": "^1.23.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onCommand:editor.showDisassembly"
    ],
    "main": "./out/extension",
    "contributes": {
        "commands": [
            {
                "command": "editor.showDisassembly",
                "title": "Show disassembly"
            }
        ]
    },
    "scripts": {
        "vscode:prepublish": "npm run compile",
        "compile": "tsc -p ./",
        "watch": "tsc -watch -p ./",
        "postinstall": "node ./node_modules/vscode/bin/install",
        "test": "npm run compile && node ./node_modules/vscode/bin/test"
    },
    "devDependencies": {
        "@types/mocha": "^2.2.42",
        "@types/node": "^7.0.43",
        "@types/underscore": "^1.8.8",
        "tslint": "^5.8.0",
        "typescript": "^2.6.1",
        "vscode": "^1.1.6"
    },
    "dependencies": {
        "underscore": "^1.9.1"
    }
}
